# Pinned to versions tested locally on Python 3.13.
# Loosen if installing on an older Python — transformers/torch wheels are
# the most likely to need adjustment.

pandas==2.2.3
numpy==2.4.2
matplotlib==3.10.3
seaborn==0.13.2
scikit-learn==1.7.2
transformers==4.57.3
sentence-transformers==5.2.0
ollama==0.6.1
streamlit==1.48.1
plotly==6.3.0
pytest==9.0.2
